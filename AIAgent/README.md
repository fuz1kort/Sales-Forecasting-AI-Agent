# Sales Forecasting AI Agent 🤖📊

Интеллектуальный агент для анализа и прогнозирования продаж с использованием ансамбля моделей машинного обучения (Prophet + SARIMA + CatBoost).

## ✨ Возможности

- **📈 Прогнозирование продаж** - Ансамбль из 3 моделей для точных прогнозов
- **🧪 Анализ стационарности** - ADF-тест для проверки временных рядов
- **📊 Визуализация данных** - Корреляции, распределения, ABC-анализ, временные ряды
- **🏆 Сравнение моделей** - Бэктестирование и выбор лучшей модели
- **📦 Анализ товаров** - KPI, тренды, сезонность, топ-продукты
- **🔄 Интерактивный чат** - Общение с агентом на естественном языке

## 🛠️ Системные требования

- **Python**: 3.9+
- **Оперативная память**: 4GB+ (рекомендуется 8GB+)
- **Дисковое пространство**: 2GB+ для зависимостей и данных
- **ОС**: Windows 10+, macOS 10.15+, Linux Ubuntu 18.04+

## 📦 Установка

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd Sales-Forecasting-AI-Agent/AIAgent
```

### 2. Создание виртуального окружения

```bash
# Windows
python -m venv .venv
.venv\Scripts\activate

# Linux/macOS
python -m venv .venv
source .venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Установка дополнительных зависимостей

```bash
pip install catboost
```

### 5. Настройка переменных окружения

Создайте файл `.env` в корневой директории проекта:

```bash
cp .env.example .env
```

Или настройте переменные окружения вручную (см. `.env.example`).

### 6. Быстрый запуск

Используйте скрипты для автоматического запуска:

```bash
# Linux/macOS
./start.sh

# Windows
start.bat
```

Скрипты автоматически:
- Активируют виртуальное окружение
- Проверяют наличие `.env` файла
- Запускают backend и frontend
- Проверяют работу компонентов

## 🚀 Запуск приложения

### Способ 1: Быстрый запуск (рекомендуется)

Используйте готовые скрипты для автоматического запуска:

**Windows:**
```cmd
start.bat
```

**Linux/macOS:**
```bash
chmod +x start.sh
./start.sh
```

### Способ 2: Ручной запуск

1. **Запуск Backend (FastAPI)**:
```bash
# Активируйте виртуальное окружение
.venv\Scripts\activate  # Windows
# или
source .venv/bin/activate  # Linux/macOS

# Запустите backend
python backend/main.py
```

2. **Запуск Frontend (Streamlit)** в новом терминале:
```bash
# Активируйте виртуальное окружение
.venv\Scripts\activate  # Windows
# или
source .venv/bin/activate  # Linux/macOS

# Запустите frontend
python -m streamlit run frontend/app.py
```

### Способ 2: Скачивание данных (опционально)

Если нужно скачать тестовый датасет:

```bash
python download_dataset.py
```

## 🌐 Доступ к приложению

После запуска откройте браузер:

- **Frontend (Streamlit)**: http://localhost:8501
- **Backend API (FastAPI)**: http://localhost:8000
- **API документация**: http://localhost:8000/docs

## 💬 Использование

### Основные команды для агента:

1. **Прогнозирование**:
   - "Сделай прогноз продаж на 30 дней"
   - "Прогнозируй продажи на следующий месяц"

2. **Анализ данных**:
   - "Покажи корреляции между продажами и ценами"
   - "Проанализируй тренды и сезонность"
   - "Проведи ABC-анализ товаров"

3. **Визуализация**:
   - "Визуализируй временные ряды продаж"
   - "Покажи распределение продаж"
   - "Отобрази топ-10 самых продаваемых товаров"

4. **Статистический анализ**:
   - "Проверь стационарность данных"
   - "Сравни модели прогнозирования"

5. **KPI и метрики**:
   - "Покажи ключевые показатели продаж"
   - "Проанализируй эффективность товаров"

### Режимы работы агента:

- **CodeAgent** (умнее): Может выполнять Python код для сложных вычислений
- **ToolCallingAgent** (быстрее): Использует только предопределенные инструменты

## 🏗️ Архитектура проекта

```
AIAgent/
├── backend/                 # FastAPI сервер
│   ├── agent/              # AI агент (SmolAgents)
│   │   ├── tools/          # Инструменты агента
│   │   │   ├── analytics/  # Аналитические инструменты
│   │   │   └── forecast/   # Инструменты прогнозирования
│   │   └── models/         # Модели LLM
│   ├── models/             # Модели ML (Prophet, SARIMA, CatBoost)
│   ├── services/           # Бизнес-логика
│   └── config/             # Конфигурация
├── frontend/               # Streamlit интерфейс
├── requirements.txt        # Зависимости Python
├── .env.example           # Пример конфигурации окружения
├── start.sh               # Скрипт быстрого запуска (Linux/macOS)
├── start.bat              # Скрипт быстрого запуска (Windows)
└── README.md              # Эта документация
```

## 🔧 Настройка и конфигурация

### Добавление новых инструментов

1. Создайте новый файл в `backend/agent/tools/`
2. Используйте декоратор `@tool` из SmolAgents
3. Добавьте импорт в `backend/agent/tools/__init__.py`
4. Обновите промпт агента в `backend/agent/smol_agent.py`

### Добавление новых моделей

1. Создайте функцию в `backend/models/`
2. Добавьте импорт в `backend/models/__init__.py`
3. Обновите сервисы в `backend/services/`

## 🐛 Возможные проблемы и решения

### Проблема: "ModuleNotFoundError" при запуске

**Решение**: Убедитесь, что виртуальное окружение активировано:
```bash
.venv\Scripts\activate  # Windows
source .venv/bin/activate  # Linux/macOS
```

### Проблема: Backend не запускается

**Решение**: Проверьте переменные окружения в `.env` файле

### Проблема: Ошибка импорта catboost

**Решение**: Установите catboost отдельно:
```bash
pip install catboost
```

### Проблема: Недостаточно памяти

**Решение**: Для больших датасетов увеличьте оперативную память или используйте меньший размер выборки

### Проблема: API ключи не работают

**Решение**: Проверьте правильность API ключей и доступность сервисов:
- YandexGPT: https://yandex.cloud/ru/docs/foundation-models/
- OpenAI: https://platform.openai.com/

## 📊 Модели прогнозирования

### Prophet (Facebook)
- Преимущества: Хорошо работает с сезонностью и трендами
- Использование: Автоматическое обнаружение сезонных паттернов

### SARIMA (Statsmodels)
- Преимущества: Классический статистический подход
- Использование: Стационарные временные ряды

### CatBoost (Yandex)
- Преимущества: Градиентный бустинг, устойчив к переобучению
- Использование: Сложные нелинейные зависимости

### Ансамбль
- Комбинирует все 3 модели для повышения точности
- Автоматический выбор лучшей модели

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создайте feature branch: `git checkout -b feature/new-feature`
3. Commit изменения: `git commit -am 'Add new feature'`
4. Push в branch: `git push origin feature/new-feature`
5. Создайте Pull Request

## 📄 Лицензия

MIT License - см. файл LICENSE для подробностей.

## 📞 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте раздел "Возможные проблемы и решения"
2. Создайте Issue в репозитории
3. Опишите вашу проблему с максимальными деталями

---

**Приятного использования! 🚀**

*Sales Forecasting Agent v4.0 - Powered by SmolAgents + Ensemble Models*</content>
<parameter name="filePath">C:\Users\gafar\source\fuz1kort\Sales-Forecasting-AI-Agent\AIAgent\README.md